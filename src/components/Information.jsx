import React, { useState } from "react";
import "./Image.css";

const Information = ({ effect }) => {
  return (
    <div>
      <div className="effect">{effect}</div>
    </div>
  );
};

export default Information;
